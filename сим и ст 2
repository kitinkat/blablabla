int main() {
    int N;
    printf("Введите целое число N (32 ≤ N ≤ 126): ");
    scanf("%d", &N);
    if (N >= 32 && N <= 126) {
        printf("Символ с кодом %d: '%c'\n", N, (char)N);
    } else {
        printf("Ошибка: число должно быть в диапазоне 32-126\n");
    }
    
    return 0;
}
