int main() {
    double a, S;
  
    printf("Введите длину стороны квадрата a: ");
    scanf("%lf", &a);
    
    if (a <= 0) {
        printf("Ошибка: сторона квадрата должна быть положительным числом!\n");
        return 1;
    }
    
    S = a * a;

    printf("Площадь квадрата со стороной %.2f равна: %.2f\n", a, S);
    
    return 0;
